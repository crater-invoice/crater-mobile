import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    mainContainer: {
        paddingHorizontal: 20,
        marginTop: 15
    },
    selectedField: {
        paddingLeft: 52
    }
});
