import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    inputGroup: {
        marginVertical: 10
    },
    addressField: {
        marginTop: -10,
        borderTopWidth: 0
    },
    line: theme => ({
        borderTopWidth: 0.4,
        borderBottomWidth: 0.4
    })
});
