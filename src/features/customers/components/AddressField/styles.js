import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        marginTop: 10,
    },
    addressValueStyle: {
        paddingLeft: 10,
    }
});
