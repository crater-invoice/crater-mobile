import { StyleSheet } from 'react-native';
import { colors } from '@/styles';

export default styles = StyleSheet.create({
    selectPicker: {
        marginTop: 12,
        marginBottom: 2
    },

    headerTitle: {
        fontSize: 18,
        marginLeft: -5
    }
});
